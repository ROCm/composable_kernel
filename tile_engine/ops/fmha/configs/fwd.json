{
  "variant": "fwd",
  "trait_config": {
    "data_type": {"values": ["fp16", "bf16"]},
    "pipeline": {"values": ["qr", "qr_async"]},
    "mask": {"values": ["no", "top_left"]},
    "bias": {"values": ["no"]}
  }
}
